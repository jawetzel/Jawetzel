# Build context is the repo root (Railway has no per-service root dir field),
# but the only Docker image we build from this repo is worker/. Exclude
# everything else so the Docker daemon doesn't receive the entire Next.js
# project as context for each build.
*
!worker/
